import React from 'react'

function Reducer(state, action) {
    switch(action.type) {
        default: 
        return state;
    }
    return (
        <div>
            
        </div>
    )
}

export default Reducer
